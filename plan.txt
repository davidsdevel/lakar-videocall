// Login

Check user credentials
if pass
  encode user ID on JWT
  store it in a cookie
else
  return unauthorize



//to checkAuth
decode JWT
check userID

if auth
  show main
else
  show dashboard




// on user dashboard

show friends connected
show config
show call icon

// to add friend

send invitation url
find by id



//Socket Events

on call
on invite
on acceptInvitation
on online


// DB tables

users

{
  name: String
  email: String
  password: String
  friends: Array<String>,
  online: Boolean
}

candidates //Show online status


//API

//User
  Create
    POST /api/user 
    body User
    return {
      status
      _id
    }
  Get
    GET /api/user/:id
    return User
  Update
    PATCH /api/user/:id
  Friends
    GET /api/user/:id/friends
  Get Single Friend
    GET /api/user/:id/friends/:friendId
