const {Router} = require('express');

Router
  .post('/', (req, res) => {

  })
  .get('/:id', (req, res) => {

  })
  .patch('/:id', (req, res) => {

  })
  .get('/:id/friends', (req, res) => {

  })
  .get('/:id/friends/:friendID', (req, res) => {
    
  });


module.exports = Router;
